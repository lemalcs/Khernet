namespace Khernet.UI
{
    /// <summary>
    /// The result of a dialog when it is closed.
    /// </summary>
    public enum DialogResult
    {
        Accept = 0,
        Cancel = 1,
        Error = 2
    }
}
